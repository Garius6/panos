# Language tour

This single-file program models tasks with a structure and an enum, stores
them in an array, keeps a small summary in a map, and uses `выбор` to turn a
status into a number.

```sh
./panos demo/01-language-tour/main.ps
```

Expected result: `12`.

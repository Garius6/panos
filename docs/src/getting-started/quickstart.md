# Быстрый старт

## Hello world локально

Точка входа любой программы — функция `старт`. Она возвращает значение
последнего выражения тела:

```panos
функ старт() -> Число
    10 + 20
конец
```

Сохраните это в `hello.ps` и запустите:

```sh
./panos hello.ps
```

```text
30
```

Явный `возврат` работает как обычно, если нужен ранний выход (этот и все
следующие примеры в документации можно редактировать и запускать прямо на
странице — кнопка «▶ Запустить»):

```panos
функ проверить(x: Число) -> Строка
    если x < 0 тогда
        возврат "отрицательное"
    конец
    "неотрицательное"
конец

функ старт() -> Строка
    проверить(-5)
конец
```

## Hello world в браузере

Ставить ничего не нужно — [веб-песочница](https://garius6.github.io/panos/playground/)
компилирует и исполняет panos прямо в браузере (тот же компилятор и VM,
собранные в WASM). Там же по умолчанию открыт `test.ps` — файл-пример,
который использует практически все конструкции языка сразу: структуры,
интерфейсы, дженерики, ADT и `выбор`, `Опция`/`Результат`, циклы, замыкания.
Хороший способ посмотреть, «как это всё выглядит вместе», не читая
документацию по главам.

Ограничения браузерной версии (в отличие от нативного CLI):

- Нет **локальных файловых импортов** — своего второго `.ps`-файла в
  песочницу не положить (один текстовый буфер). Стандартная библиотека
  (`математика`, `коллекции`, `строки` и т.д.) при этом работает — она
  вшита прямо в WASM-сборку. Подробности и точный список — в разделе
  [«Модули» → ограничение в браузере](../language/modules.md#ограничение-в-браузере).
- Нет доступа к файловой системе/сети/stdin — `фс`, `ос`, `ввод_вывод`,
  `сеть` импортируются без ошибки, но вызов их функций падает в панику во
  время выполнения.

## Дальше

- [Установка](./installation.md), если ещё не поставили `panos`/`panos-lsp`
  локально.
- [Использование CLI](./cli.md) — флаги, аргументы программы, поиск модулей.
- Раздел **Язык**, начиная с [базовых типов и операторов](../language/basic-types.md) —
  последовательный тур по синтаксису.
- `test.ps` в корне репозитория — тот же файл, что открыт в песочнице по
  умолчанию, можно запускать локально: `./panos test.ps`.

импорт "./generic_cross_module_fixture_lib" как либ

тип Точка = структура
	x: Число
конец

функ старт() -> Булево
	пер точки = массив(Точка(5), Точка(1))
	пер результат = либ.применить_ко_всем(точки, функ(a: Точка) -> Булево a.x > 0 конец)

	результат[0].x == 5
конец

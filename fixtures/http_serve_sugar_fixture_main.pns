импорт "сеть/http" как хттп

функ обработчик(запрос: Запрос) -> хттп.ОтветСервера
    хттп.ОтветСервера(200.0, "text/plain", "sugar:" + запрос.путь())
конец

функ старт() -> Число
    хттп.обслуживать(18190.0, обработчик, 1.0).ожидать("не смог слушать")
конец

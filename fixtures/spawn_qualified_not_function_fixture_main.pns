// Стадия 45: негатив — `запусти Модуль.Экспорт(...)`, где Экспорт
// существует, но не функция (тут — тип-перечисление), даёт понятную
// Type Error, не крэш компилятора.
импорт супервизор

функ старт() -> Пусто
    запусти супервизор.Стратегия()
конец

// No `импорт` at all — `Опция` comes from the embedded prelude, merged
// unqualified into every module (see `module_loader.Graph.appendPreludeModule`).
экспорт функ старт() -> Число
    пер о: Опция(Число) = Опция.Есть(42)
    выбор о
        Есть(x) -> x
        Нет -> 0
    конец
конец

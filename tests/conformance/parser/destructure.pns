тип Точка = структура
    x: Число
    y: Число
конец

функ сумма_точек() -> Число
    пер (лево, право) = (1, 2)
    пер Точка(x: абсцисса, y: ордината) = Точка(3, 4)
    конст Точка(первый, второй) = Точка(5, 6)
    лево + право + абсцисса + ордината + первый + второй
конец
